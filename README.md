# ТЕХНІЧНИЙ ЗВІТ З ЛАБОРАТОРНОЇ РОБОТИ №3
## ТЕМА: АРХІТЕКТУРА ТА ПОГЛИБЛЕНЕ ВИВЧЕННЯ ІНСТРУМЕНТАРІЮ CLI В ОС LINUX
**Категорія:** Системне адміністрування та автоматизація Bash  
**Виконавець:** Виноградов Антон  

---

## 1. КОНЦЕПТУАЛЬНИЙ ТЕОРЕТИЧНИЙ БАЗИС

### 1.1 Мета та об'єкт дослідження
Метою даної науково-практичної роботи є вивчення фундаментальних принципів взаємодії користувача з ядром операційної системи через командний інтерпретатор **Bash (Bourne Again SHell)**. В ході роботи досліджується логіка обробки текстових потоків, механізми управління змінними оточення, створення користувацьких макросів (аліасів) та розробка скриптових модулів для автоматизації рутинних адміністративних задач.

### 1.2 Глосарій розширеної термінології

Нижче наведено структурований словник базових дефініцій, що використовуються в архітектурі Unix-подібних систем:

| Технічний термін | Семантичне значення | Функціональна роль у системі |
|:-----------------|:--------------------|:-----------------------------|
| **Command** | Системний виклик | Атомарна інструкція, що ініціює виконання двійкового коду або вбудованої функції. |
| **CLI (Interface)** | Текстова оболонка | Метод керування ресурсами без залучення графічного стека (X11/Wayland). |
| **Kernel Shell** | Інтерпретатор | Програмний прошарок, що виступає парсером команд між користувачем та ядром. |
| **Prompt (PS1)** | Маркер готовності | Рядок динамічного запиту, що відображає метадані поточної сесії (User, Host, PWD). |
| **Option/Flag** | Модифікатор логіки | Ключі (Short/Long options), що змінюють параметри виконання утиліти. |
| **Alias** | Псевдонім | Метод абстракції складних командних ланцюгів у короткі ідентифікатори. |
| **Man Pages** | Технічний репозиторій | Локальна база документації, що містить специфікації до кожної системної утиліти. |
| **Directory/Path** | Ієрархія ФС | Адресація об'єктів у деревоподібній структурі файлової системи Linux. |

---

## 2. АНАЛІТИЧНИЙ РОЗБІР КОМАНДНОГО СТЕКА

### 2.1 Ієрархія та типи команд у Bash
В системі Linux виділяють чотири основні класи команд, кожна з яких має свій пріоритет виконання:
1.  **Aliases:** Користувацькі скорочення (найвищий пріоритет).
2.  **Functions:** Локальні процедури, оголошені в сесії або `.bashrc`.
3.  **Built-ins:** Команди, вбудовані безпосередньо в код оболонки (напр., `cd`, `pwd`, `echo`).
4.  **External Programs:** Бінарні файли, розташовані в директоріях, вказаних у змінній `$PATH` (напр., `/usr/bin/ls`).

### 2.2 Управління параметрами та аргументами
Кожна команда в CLI-режимі підпорядковується стандарту POSIX:
* **Опції** (наприклад, `ls -la`) — змінюють "як" працює команда.
* **Аргументи** (наприклад, `ls /home/user`) — вказують "над чим" виконується дія.

---

## 3. ПРАКТИЧНИЙ ПРОТОКОЛ ТА ХІД РОБОТИ

### 3.1 Моделювання середовища та робота зі змінними
Змінні в Bash є фундаментальним інструментом для передачі даних між процесами. Ми ініціалізували три строкові змінні для імітації бази даних користувачів.

```bash
# --- Етап 1: Ініціалізація локальних змінних ---
# Створюємо змінні для ідентифікації учасників групи
user_node_01="Anton"

# --- Етап 2: Верифікація даних через стандартний вивід ---
# Використовуємо команду echo з інтерполяцією змінних
echo "Ініціалізовано записи для вузлів: $user_node_01

# --- Етап 3: Аудит системних змінних оточення ---
# Перегляд поточної директорії та імені хоста
echo "Поточна локація сесії: $PWD"
echo "Активний користувач у системі: $USER"
    * `locate` використовує індексовану базу даних для миттєвого пошуку по всьому дереву каталогів.
3.  **Змінна `$PS1`:** Визначає структуру рядка запиту. Вона дозволяє динамічно відображати поточний час, шлях або статус попередньої команди.

# Створення шорткатів для виклику історичних календарних даних
alias get_cal_2007="cal 2007"

# Тестування псевдоніма: виклик календаря за 2007 рік одним словом
get_cal_2007
# --- Оголошення функції аналітичного звіту ---
# Ця функція форматує дані та виводить часову мітку операції через $(date)
generate_system_report() {
  echo "------------------------------------------"
  echo "   ПРОТОКОЛ ОБРОБКИ СТУДЕНТСЬКОЇ ГРУПИ    "
  echo "------------------------------------------"
  echo "Вузол Gamma: $user_node_03 | Стек: 2007"
  echo "------------------------------------------"
  echo "Дата генерації логу: $(date)"
}

# Активація модуля в поточному терміналі
generate_system_report

# Оголошення масивів вихідних даних (Array)
names_array=("Антон")
years_array=(2007)

# Виконання ітераційного циклу для обробки кожного елемента масиву за індексом
for i in ${!names_array[@]}; do
  echo "Аналіз профілю вузла [$i]: Користувач: ${names_array[$i]}, Рік: ${years_array[$i]}"
done
---

## ❓ 5. АНАЛІТИЧНІ ВІДПОВІДІ НА КОНТРОЛЬНІ ЗАПИТАННЯ

У ході теоретичного опрацювання матеріалу та практичного тестування команд в ОС Linux було сформувано наступні висновки щодо архітектури Bash:

1.  **Типологія команд у середовищі Bash:** Командна оболонка розрізняє чотири основні категорії інструментів, кожна з яких має свій пріоритет обробки:
    * **Aliases (Псевдоніми):** Користувацькі скорочення для складних команд.
    * **Functions (Функції):** Скриптові блоки, що зберігаються в оперативній пам'яті сесії.
    * **Built-ins (Вбудовані команди):** Утиліти, що інтегровані в ядро самого інтерпретатора (напр., `cd`, `echo`).
    * **External Programs (Зовнішні програми):** Бінарні файли, що зберігаються за системними шляхами (напр., `/usr/bin/ls`).

2.  **Функціональне призначення змінної оточення `$PS1`:**
    Дана змінна є конфігураційним дескриптором, що визначає візуальну структуру рядка запрошення (Prompt). Вона дозволяє адміністратору динамічно відстежувати метадані сесії: ім'я хоста (`\h`), поточного користувача (`\u`), час виконання операції або повний шлях до робочого каталогу (`\w`).

3.  **Диференціація механізмів екранування (Quotes):**
    В ОС Linux вибір типу лапок критично впливає на інтерпретацію тексту:
    * **Подвійні лапки (`" "`)**: Дозволяють системі розпізнавати службові символи та підставляти значення змінних (інтерполяція).
    * **Одинарні лапки (`' '`)**: Сприймають кожен символ як статичний літерал, блокуючи будь-яку обробку змінних чи команд усередині рядка.

4.  **Специфікація пошукових утиліт `whereis` та `locate`:**
    * `whereis` — фокусується виключно на пошуку двійкових файлів, вихідних кодів та мануалів у стандартних системних директоріях.
    * `locate` — використовує попередньо індексовану базу даних (`mlocate.db`) для миттєвого пошуку будь-яких об'єктів у всій файловій ієрархії, що значно швидше за прямий перебір дисків.

5.  **Рівні привілеїв та системні ідентифікатори:**
    Кінцевий символ у промпті дозволяє миттєво визначити рівень доступу: символ `$` вказує на обмежену сесію звичайного користувача, тоді як `#` сигналізує про повні адміністративні права (Root/Superuser).

---

## ✅ ЗАГАЛЬНИЙ ВИСНОВОК

У ході виконання лабораторної роботи №3 було проведено комплексне дослідження архітектурних особливостей та функціональних можливостей командного інтерфейсу ОС Linux. На практичному рівні було успішно реалізовано алгоритми управління сесіями через локальні змінні, оптимізовано взаємодію з терміналом за допомогою системи аліасів та розроблено програмні модулі на базі функцій та ітераційних циклів Bash.

Встановлено, що використання CLI-режиму (Command Line Interface) забезпечує значно вищу продуктивність та гнучкість у задачах системного адміністрування порівняно з графічними оболонками (GUI). Отримані знання сформували фундаментальну базу для подальшого вивчення автоматизації серверних процесів, розробки складних системних скриптів та безпечного конфігурування мережевих платформ.

*The completion of laboratory work No. 3 allowed for a comprehensive study of the architectural features and functional capabilities of the Linux command line interface. On a practical level, session management algorithms through local variables were successfully implemented, terminal interaction was optimized using a system of aliases, and software modules based on Bash functions and iterative loops were developed. The skills gained form a fundamental basis for further study of server process automation and professional system configuration.*
